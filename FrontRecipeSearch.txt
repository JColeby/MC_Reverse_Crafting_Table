CREATE VIEW front_recipe_search AS
SELECT r.recipeid, r.itemid AS recipeItemid, i.itemid AS ingredientitemid, recipetype, pattern, i.patternkey 
FROM recipe AS r JOIN ingredient AS i ON r.recipeid = i.recipeid;
